# Core ML
numpy>=1.26,<3
Pillow>=10.0
PyYAML>=6.0

# PyTorch
# Install the CUDA-compatible PyTorch build separately when needed.
torch>=2.2
torchvision>=0.17

# Diffusion / model tooling
diffusers>=0.35.0
transformers>=4.45.0
accelerate>=0.34.0
safetensors>=0.4.5
huggingface_hub>=0.25.0
peft>=0.13.0

# Dataset / image processing
datasets>=2.20.0
opencv-python-headless>=4.10.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Validation / testing
pydantic>=2.8.0
pytest>=8.0.0
